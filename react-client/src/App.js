import './style.css';

function App() {
  return (
    <div className="App">
      <h1>Deutsche Bank Project</h1>
        <p>The basic file structure has been setup; but feel free to change it as you wish.</p>
        <p>This can be run in docker etc and part of docker compose; but just for testing - I recommend
        simply running it on your machine for now: `npm install` to get node modules then `npm start`.</p>
    </div>
  );
}

export default App;
